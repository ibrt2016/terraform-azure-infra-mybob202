apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: "783443"
  annotations:
    github.com/project-slug: ibrt2016/terraform-azure-infra-mybob202 # Change the value to your github repo which contains terraform code.
    backstage.io/techdocs-ref: dir:.
  links:
  - url: https://github.com/claranet/terraform-azurerm-vnet
    title: 'VNet Terraform Module'
    icon: github
  tags:
    - azure
    - vnet
    - brand-new-deployment
    - critical
    - mybob202
    - prod
  title: 'Azure reource group and VNet - Ticket #783443' # you can give any title
  description: |
   Azure reource group and VNet in stack `mybob202` in `prod` environment\
   \
   Ticket number: `783443`\
   Details: Create `Azure VNet` and a `resource group` in a `brand new` stack `mybob202`, in `prod` environment and in `us-east` region\
   Action: Create a `new resource group` and a `new VNet`\
   Brand new deployment: `Yes`\
   VNet address space: `10.30.0.0/16`\
   VNet suffix: `001`"
   Stack name: `mybob202`
   Stack owner: `Bob Tayara`
   Azure subscription: `infra`
   Criticality: `High`
   Entity: `US`
   Cost center: `00005` 
spec:
  type: service
  owner: user:guest
  lifecycle: experimental